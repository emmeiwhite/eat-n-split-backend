## Routes and Controllers

After setting the routes and business logic (Controllers) for each route.
Now any request like:

- POST http://localhost:5000/api/auth/register
- POST http://localhost:5000/api/auth/login

will hit our controllers. (We'll create these from the request handlers)
